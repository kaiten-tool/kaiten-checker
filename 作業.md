# 作業記録（kaiten-checker）

## 2026-09-24

### 概要
回転率チェッカー（`app.py`）の不具合修正、スマホでの使い勝手改善、見切り判断を助ける機能の追加を行った。
変更は [PR #1](https://github.com/kaiten-tool/kaiten-checker/pull/1) で `main` にマージ済み。

### 不具合修正
- **自動復元できず、前回の入力が空データで上書きされる問題**
  - 原因：アプリを開いた直後の1回目の処理では、ブラウザ（localStorage）の値がまだ届いておらず空になる。それを「保存データなし」と判断して復元をやめ、さらに空のデータで上書き保存していた。
  - 対応：ブラウザから値が届いた2回目の処理まで、復元と自動保存を待つようにした。
- **保存済みデータが再接続で消える問題**
  - 「この実戦を保存して次へ」で保存した実戦（サマリー・詳細）もlocalStorageへ自動保存し、開き直したときに復元するようにした。
  - 全削除した場合もlocalStorageへ反映する。
- **実戦結果の入力値が0に戻る問題**
  - 「直前の1行を削除」で1k確定がなくなり入力欄が非表示になると、初当たり回数・獲得玉数などが消えていた。非表示の間も値を保持するようにした。

### スマホでの使い勝手改善
- 区間開始・1k確定のあと、現在回転数の入力欄を空にする（続けて数字だけ打てる）。
- 画面の並びを「保存名・ボーダー → 現在回転数 → ボタン → 現在の結果 → 入力履歴（新しい順）」に変更。
- 現在の結果を3列×2段のカード表示に変更。1k確定ボタンを赤で強調。
- 今回1kが8回転未満または45回転超のときは「入力ミスではありませんか？」と確認してから記録。
- 「保存済みデータを全削除」に確認を追加。
- 画面上部の説明文を今の入力方式に合わせて更新。

### 見切り判断のための機能
- **ボーダー入力欄**
  - 累計/k・直近5k・直近10kとボーダーの差を色付きで表示（緑：上回り、赤：下回り）。
  - ボーダーは次の実戦へ引き継ぎ、保存済みサマリー・CSVにも「ボーダー」「ボーダー差（累計/k）」を保存。
- **見切りの目安**（5k以上で判定）

  | 表示 | 条件 |
  |---|---|
  | 見切り | 直近5kと、その前の5kが、2回続けてボーダー−2以下 |
  | 見切り検討 | 直近10kがボーダー−2以下 |
  | 注意 | 直近5kがボーダー−2以下 |
  | 良好 | 直近5k・10kがどちらもボーダー以上 |
  | 様子見 | 上のどれにも当てはまらない |

- **ボーダー差グラフ**
  - 各時点の直近5k（青）・直近10k（オレンジ）・累計（紫）の「平均−ボーダー」を表示。
  - 縦軸は±5回転固定。超えた点は▲▼で上端・下端に表示。
  - 灰色の点線＝ボーダー、赤の点線＝見切りライン（−2）。
  - 途中で検討した「ボーダー差の累積グラフ」は、打つほど値が大きくなりスマホで見づらいため、この形に置き換えた。

### 設定値（`app.py` 冒頭）
| 名前 | 値 | 意味 |
|---|---|---|
| `SUSPICIOUS_MIN_1K` / `SUSPICIOUS_MAX_1K` | 8 / 45 | この範囲外の今回1kは確認してから記録 |
| `CUTOFF_DIFF` | 2.0 | 見切りの目安に使う「ボーダー−○」 |
| `CHART_DIFF_RANGE` | 5.0 | ボーダー差グラフの縦軸の幅（±） |

### 回転率の見方（話し合った内容）
- 1kごとの回転数はブレが大きい（仮に±3回転なら、5k平均で±1.3、10k平均で±1、30k平均で±0.5程度）。
- 途中で回転率が変わる台では、累計/kは遅れて動く。今後を判断するには、直近5k・10kをボーダーと直接比べる。
- 例：20kまで19.5の台が30k時点で累計18.2 → 後半10kは15.6。偶然ではなく台が変わった可能性が高い。新しい目安では25kで「注意」、30kで「見切り」になる。
- 記録するタイミング（保留を消化しきってから入力する等）をそろえると、数値が安定する。

### 確認方法
- Streamlit AppTest（localStorageを模した部品で置き換え）で、復元・保存済みデータ・実戦結果の保持・新機能を確認。
- スマホ幅（390px）のChromiumで実際のアプリを操作し、再読み込み後の復元と表示を確認。
- iPhoneのSafariでの動作は未確認。

### 注意・今後の候補
- 修正前のアプリで保存したデータは、不具合のため消えている可能性がある。
- 保存先はその端末のそのブラウザのみ。大事なデータはCSVでダウンロードしておく。
- Streamlitから `use_container_width` は将来使えなくなるという警告が出ている（`width='stretch'` への置き換えが推奨）。現状の動作には影響なし。
- 実際に使ってみて、見切りライン（−2）や確認範囲（8〜45回転）が合わなければ調整する。
- 見送った案：レート切り替え（1円パチンコ対応）、保存件数の表示・容量警告。
